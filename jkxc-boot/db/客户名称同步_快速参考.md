# 客户名称同步更新 - 快速参考卡片

## 🚀 5分钟快速上手

### 1️⃣ 备份（必须！）
```bash
mysqldump -u root -p jkxc-cg > backup.sql
```

### 2️⃣ 执行主脚本
```bash
mysql -u root -p jkxc-cg < sync_customer_name_update.sql
```

### 3️⃣ 验证结果
```bash
mysql -u root -p jkxc-cg < customer_name_sync_maintenance.sql
```

---

## 📁 文件速查

| 需求 | 使用文件 |
|------|----------|
| 首次部署 | `sync_customer_name_update.sql` |
| 日常检查 | `customer_name_sync_maintenance.sql` |
| 测试验证 | `test_customer_name_sync.sql` |
| 操作手册 | `客户名称同步_执行指南.md` |
| 技术方案 | `客户名称同步更新方案.md` |

---

## 🔍 常用SQL命令

### 查看触发器
```sql
SHOW TRIGGERS WHERE `Trigger` = 'trg_customer_name_update';
```

### 检查数据一致性
```sql
SELECT COUNT(*) FROM gh_order o
INNER JOIN gh_customer c ON o.company_id = c.id
WHERE o.company_name != c.corporate_name AND o.del_flag = 0;
```

### 手动修复单个客户
```sql
SET @customer_id = 'YOUR_CUSTOMER_ID';
SET @new_name = (SELECT corporate_name FROM gh_customer WHERE id = @customer_id);

UPDATE gh_order 
SET company_name = @new_name, update_time = NOW()
WHERE company_id = @customer_id AND del_flag = 0;
```

### 删除触发器（回滚）
```sql
DROP TRIGGER IF EXISTS trg_customer_name_update;
```

---

## ⚠️ 注意事项

### ✅ 必须做
- 执行前备份数据库
- 在测试环境先验证
- 选择业务低峰期执行

### ❌ 不要做
- 不要跳过备份步骤
- 不要在生产环境直接测试
- 不要在业务高峰期执行

---

## 🐛 故障速查

| 问题 | 解决方案 |
|------|----------|
| 触发器创建失败 | `SET GLOBAL log_bin_trust_function_creators = 1;` |
| 修复很慢 | 分批执行，每次1000条 |
| 数据不一致 | 重新执行修复脚本 |
| 性能影响大 | 检查索引，考虑异步更新 |

---

## 📊 涉及的表（9个）

1. gh_order
2. gh_accounting_contract
3. gh_accounting_handover
4. gh_business_task
5. gh_address_center
6. gh_address_renew
7. gh_bank_diary
8. gh_reimbursement
9. gh_opportunity

---

## 📞 获取帮助

1. 查看 `客户名称同步_执行指南.md` 中的"常见问题处理"
2. 执行 `customer_name_sync_maintenance.sql` 生成诊断报告
3. 查看MySQL错误日志

---

## 🔄 维护计划

| 频率 | 任务 | 耗时 |
|------|------|------|
| 每周 | 数据一致性检查 | 5分钟 |
| 每月 | 性能评估 | 30分钟 |
| 每季度 | 全面审计 | 1-2小时 |

---

## 📈 性能参考

- 单次更新：< 100ms
- 历史修复（1万条）：< 10秒
- 一致性检查：< 5秒

---

**快速链接：**
- [详细执行指南](./客户名称同步_执行指南.md)
- [技术方案说明](./客户名称同步更新方案.md)
- [完整文档导航](./客户名称同步_README.md)

---

**版本：** v1.0 | **更新：** 2026-03-01
